<form method="post" action="{{ path('app_tarefa_delete', {'id': tarefa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarefa.id) }}">
    <button class="btn">
            <button class="btn {{ button_color|default('btn-outline-danger') }}  mt-3">{{ button_label|default('Excluir') }}</button>

    </button>
</form>
